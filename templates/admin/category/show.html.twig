{% extends 'base_admin.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <a href="{{ path('admin_category_index') }}" class="underline text-blue-500"><i class="fas fa-arrow-left"></i> Retour</a>
    {{ include('admin/layout/show.html.twig', {
        title: 'Category',
        entity: category,
        label : ["Nom", "Type"],
        fields : ["name", "type"],
        edit: "admin_category_edit"
    }) }}
    
    {{ include('admin/category/_delete_form.html.twig') }}
{% endblock %}
