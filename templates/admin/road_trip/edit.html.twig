{% extends 'base.html.twig' %}

{% block title %}Edit RoadTrip{% endblock %}

{% block body %}
    <h1>Edit RoadTrip</h1>

    {{ include('road_trip/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('road_trip_index') }}">back to list</a>

    {{ include('road_trip/_delete_form.html.twig') }}
{% endblock %}
